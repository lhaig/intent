// stage1: entity 'P' eq method must take a parameter of type P
module ck_assert_eq_eq_wrong_param_type version "0.1.0";

entity P {
    field x: Int;

    method eq(other: Int) returns Bool {
        return true;
    }
}

function f(a: P, b: P) returns Void {
    assert_eq(a, b);
}
