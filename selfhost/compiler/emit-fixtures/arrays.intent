module arrays version "1.0.0";

entry function main() returns Int {
    let nums: Array<Int> = [10, 20, 30];
    let mutable total: Int = 0;
    let mutable i: Int = 0;
    while i < len(nums) {
        total = total + nums[i];
        i = i + 1;
    }
    return total;
}
