module enum_example version "1.0.0";

enum Status {
    Pending,
    Running,
    Complete,
}

enum Shape {
    Circle(radius: Float),
    Rectangle(width: Float, height: Float),
    Point,
}

function getStatus() returns Status {
    return Running;
}

function makeCircle(r: Float) returns Shape {
    return Circle(r);
}

function makeRectangle(w: Float, h: Float) returns Shape {
    return Rectangle(w, h);
}

function makePoint() returns Shape {
    return Point;
}

entry function main() returns Int {
    let s: Status = getStatus();
    let c: Shape = makeCircle(5.0);
    let r: Shape = makeRectangle(10.0, 20.0);
    let p: Shape = makePoint();
    print(42);
    return 0;
}

test "match on unit enum variant" {
    let s: Status = getStatus();
    let n: Int = match s {
        Pending => 0,
        Running => 1,
        Complete => 2,
    };
    assert_eq(n, 1);
}

test "match on data-carrying enum variant" {
    let c: Shape = makeCircle(5.0);
    let kind: Int = match c {
        Circle(_) => 0,
        Rectangle(_, _) => 1,
        Point => 2,
    };
    assert_eq(kind, 0);
}

